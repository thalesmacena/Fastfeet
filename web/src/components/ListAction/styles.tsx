import styled from 'styled-components';

export const ListActionContainer = styled.span`
  button {
    background: none;
    text-align: center;
    border: 0;
    transform: translateX(-50%);
  }
`;
